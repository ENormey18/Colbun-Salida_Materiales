_schema-version: "3.2"
ID: salidademateriales
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: salidademateriales-destination-content
  type: com.sap.application.content
  requires:
  - name: salidademateriales-destination-service
    parameters:
      content-target: true
  - name: salidademateriales-repo-host
    parameters:
      service-key:
        name: salidademateriales-repo-host-key
  - name: salidademateriales-uaa
    parameters:
      service-key:
        name: salidademateriales-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: salidademateriales_html_repo_host
          ServiceInstanceName: salidademateriales-html5-service
          ServiceKeyName: salidademateriales-repo-host-key
          sap.cloud.service: salidademateriales
        - Authentication: OAuth2UserTokenExchange
          Name: salidademateriales_uaa
          ServiceInstanceName: salidademateriales-xsuaa-service
          ServiceKeyName: salidademateriales-uaa-key
          sap.cloud.service: salidademateriales
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: salidademateriales-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: salidademateriales-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - salidademateriales.zip
      name: salidademateriales
      target-path: resources/
- name: salidademateriales
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: salidademateriales-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: salidademateriales-destination-service
    service-plan: lite
- name: salidademateriales-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: salidademateriales-xsuaa-service
    service-plan: application
- name: salidademateriales-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: salidademateriales-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
