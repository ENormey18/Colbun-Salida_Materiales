<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
>
    <Dialog
        title="Modificación Características Materiales"
        resizable="true"
    >
        <content>
            <VBox alignItems="Center">
                <VBox width="90%">
                    <Table
                        headerText="{detalleReserva>/itemSeleccionado/id}"
                        id="caracteristicasTable"
                        items="{path:'detalleReserva>/itemSeleccionado/caracteristicas/results'}"
                        mode="MultiSelect"
                    >
                        <columns>
                            <Column>
                                <Text text="CAT" />
                            </Column>
                            <Column>
                                <Text text="Descripción CAT" />
                            </Column>
                            <Column>
                                <Text text="Valor CAT" />
                            </Column>
                        </columns>

                        <items>
                            <ColumnListItem>
                                <cells>
                                    <Text text="{cat}" />
                                    <Text text="{descripcionCAT}" />
                                    <VBox >
                                        <Text text="{valorCAT}" visible="{= !!${valorCAT}}"/>
                                        <Input value="{newValorCAT}" editable="{= !${valorCAT}}" visible="{= !!${valorCAT}}"/>
                                    </VBox>
                                </cells>
                            </ColumnListItem>
                        </items>
                    </Table>
                </VBox>
            </VBox>
        </content>
        <beginButton>
            <Button
                text="OK"
                type="Emphasized"
                press=".onAceptarCaracteristicas"
            />
        </beginButton>
        <endButton>
            <Button
                text="Cancel"
                press=".onCancelarCaracteristicas"
            />
        </endButton>
    </Dialog>
</core:FragmentDefinition>
